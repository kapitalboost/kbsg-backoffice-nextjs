export const isMobile = (screens: any) => screens.xs

export const isTablet = (screens: any) => screens.sm && screens.md && screens.lg

export const isDesktop = (screens: any) => screens.xl || screens.xxl
